@model IEnumerable<MeatMe.Models.CutName>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CutName1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrimalCut.PrimalCutName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CutName1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrimalCut.PrimalCutName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CutId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CutId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CutId })
        </td>
    </tr>
}

</table>
